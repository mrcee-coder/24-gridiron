(()=>{const URL='https://rcaeofytdbuzejdkybcq.supabase.co',KEY='sb_publishable_AwEpAG3KIfBUCVJtwgrevg_ON-W5y_w';const menu=document.querySelector('#menu'),mobile=document.querySelector('#mobileNav');document.querySelectorAll('[data-vote]').forEach(x=>x.addEventListener('click',()=>{window.location.href='./vote.html'}));document.querySelectorAll('a[href="#games"]').forEach(a=>a.href='./games.html');if(menu&&mobile){menu.addEventListener('click',()=>{const open=mobile.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});mobile.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobile.classList.remove('open');menu.setAttribute('aria-expanded','false')}))}const games=document.querySelector('#games');if(games&&!games.querySelector('.games-hub-link')){const wrap=document.createElement('div');wrap.className='games-hub-link';wrap.innerHTML='<a class="cta ghost" href="./games.html">Open Side Games hub →</a>';games.appendChild(wrap)}const lab=document.querySelector('#lab .shell');if(lab&&!document.querySelector('#syncTitle')){const strip=document.createElement('div');strip.className='sync-strip';strip.innerHTML='<span class="sync-dot" id="syncDot"></span><div><b id="syncTitle">Sleeper connection waiting</b><small id="syncDetail">The site is ready; the league ID gets connected once the Sleeper league is active.</small></div><a href="./pressbox.html">Commissioner setup →</a>';lab.prepend(strip);sync()}async function rpc(n,b={}){const r=await fetch(`${URL}/rest/v1/rpc/${n}`,{method:'POST',headers:{'Content-Type':'application/json','apikey':KEY},body:JSON.stringify(b)});if(!r.ok)throw Error('sync unavailable');return r.json()}async function sync(){const title=document.querySelector('#syncTitle'),detail=document.querySelector('#syncDetail'),dot=document.querySelector('#syncDot');if(!title)return;try{const cfg=await rpc('gridiron_public_config');if(!cfg.sleeper_league_id){title.textContent='Sleeper connection waiting';detail.textContent='Everything is ready. The commissioner connects the numeric Sleeper league ID once the league is active.';return}title.textContent='Connecting to Sleeper…';const [league,users,rosters]=await Promise.all([fetch(`https://api.sleeper.app/v1/league/${cfg.sleeper_league_id}`).then(r=>r.json()),fetch(`https://api.sleeper.app/v1/league/${cfg.sleeper_league_id}/users`).then(r=>r.json()),fetch(`https://api.sleeper.app/v1/league/${cfg.sleeper_league_id}/rosters`).then(r=>r.json())]);if(!league||league.error)throw Error('not found');title.textContent=`Sleeper connected · ${league.name||'24 Gridiron'}`;detail.textContent=`${users?.length||0} managers · ${rosters?.length||0} rosters · ${league.status||'preseason'} · read-only sync`;dot.style.background='#70b780';dot.style.boxShadow='0 0 14px #70b780'}catch(e){title.textContent='Sleeper sync is temporarily unavailable';detail.textContent='The league site still works. Sleeper data will retry automatically on the next visit.'}}})();