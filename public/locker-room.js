(()=>{const $=s=>document.querySelector(s);const name=window.G24Identity?.get?.()||localStorage.getItem('g24_member_name')||'';if($('#lockerIdentity'))$('#lockerIdentity').textContent=name?`Locker: ${name}`:'Visitor locker';function tone(freq=100,dur=.12,type='triangle',gain=.05){try{const A=window.AudioContext||window.webkitAudioContext,a=new A(),o=a.createOscillator(),g=a.createGain();o.type=type;o.frequency.setValueAtTime(freq,a.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(40,freq*.72),a.currentTime+dur);g.gain.setValueAtTime(.0001,a.currentTime);g.gain.exponentialRampToValueAtTime(gain,a.currentTime+.01);g.gain.exponentialRampToValueAtTime(.0001,a.currentTime+dur);o.connect(g);g.connect(a.destination);o.start();o.stop(a.currentTime+dur+.02)}catch{}}document.querySelectorAll('.locker').forEach(locker=>{locker.addEventListener('mouseenter',()=>tone(72,.08,'square',.025));locker.addEventListener('click',e=>{tone(110,.16,'sawtooth',.06);const flash=document.createElement('div');flash.className='locker-open-flash';document.body.appendChild(flash);setTimeout(()=>flash.remove(),340)})});})();
