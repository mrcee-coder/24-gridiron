(()=>{const URL='https://rcaeofytdbuzejdkybcq.supabase.co',KEY='sb_publishable_AwEpAG3KIfBUCVJtwgrevg_ON-W5y_w',SLUG='preseason-effort-index-2026';const $=s=>document.querySelector(s);let state=null;async function rpc(n,b={}){const r=await fetch(`${URL}/rest/v1/rpc/${n}`,{method:'POST',headers:{'Content-Type':'application/json','apikey':KEY},body:JSON.stringify(b)});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||d.error||'Request failed');return d}const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function render(){if(!state?.poll){$('#pollState').textContent='No active ballot';return}const p=state.poll;$('#pollKicker').textContent=p.kicker||'Current ballot';$('#pollTitle').textContent=p.title;$('#pollDescription').textContent=p.description||'';$('#pollState').textContent=`${state.status.toUpperCase()} · ${state.ballots_cast} ballots cast · ${state.claimed}/${state.eligible} seats claimed`;const disabled=state.status!=='open';$('#submitPoll').disabled=disabled;$('#questions').innerHTML=p.questions.map(q=>`<section class="question"><h3>${esc(q.prompt)}</h3><p>${esc(q.subtitle||'')}</p><div class="choices">${state.members.map((m,i)=>`<div class="choice"><input type="radio" id="${esc(q.key)}-${i}" name="${esc(q.key)}" value="${esc(m)}" required/><label for="${esc(q.key)}-${i}">${esc(m)}</label></div>`).join('')}</div></section>`).join('');if(disabled)$('#pollMessage').textContent=state.status==='closed'?'Voting is closed. Results remain sealed until close.':'This ballot is not open yet.'}async function load(){try{state=await rpc('gridiron_story_poll_state',{p_slug:SLUG});render()}catch(e){$('#pollState').textContent='Poll service unavailable'}}$('#pollForm').addEventListener('submit',async e=>{e.preventDefault();const msg=$('#pollMessage'),btn=$('#submitPoll');if(!state||state.status!=='open')return;const fd=new FormData(e.currentTarget);const answers={};for(const q of state.poll.questions)answers[q.key]=fd.get(q.key);btn.disabled=true;msg.textContent='Casting ballot…';try{const out=await rpc('gridiron_story_poll_submit',{p_member_name:$('#memberName').value,p_access_code:$('#ticketCode').value,p_poll_slug:SLUG,p_answers:answers});msg.textContent=`Ballot recorded for ${out.member}. You can come back and update it before voting closes.`;await load()}catch(err){msg.textContent=err.message}finally{btn.disabled=false}});load()})();