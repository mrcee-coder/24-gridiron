(()=>{const URL='https://rcaeofytdbuzejdkybcq.supabase.co',KEY='sb_publishable_AwEpAG3KIfBUCVJtwgrevg_ON-W5y_w',MEM='g24_member_name';const $=s=>document.querySelector(s);let state=null,member='';async function rpc(n,b={}){const r=await fetch(`${URL}/rest/v1/rpc/${n}`,{method:'POST',headers:{'Content-Type':'application/json','apikey':KEY},body:JSON.stringify(b)}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||d.error||'Request failed');return d}const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function remembered(){if(window.G24Identity)return window.G24Identity.get();try{return localStorage.getItem(MEM)||''}catch{return''}}function remember(v){if(window.G24Identity){v?window.G24Identity.set(v):window.G24Identity.clear();return}try{v?localStorage.setItem(MEM,v):localStorage.removeItem(MEM)}catch{}}function labels(){return Object.fromEntries((state?.questions||[]).map(q=>[q.key,q.label]))}function questions(card={}){const a=card.answers||{};$('#dtQuestions').innerHTML=(state.questions||[]).map(q=>`<div class="dt-question"><div class="dt-qtag"><b>${esc(q.label)}</b><span>${q.points} PT${q.points===1?'':'S'} · ${esc(q.qualifier||'')}</span></div><div class="dt-qcopy"><h3>${esc(q.prompt)}</h3><p>${esc(q.detail||'')}</p></div><input data-key="${esc(q.key)}" maxlength="80" autocomplete="off" placeholder="Player name" value="${esc(a[q.key]||'')}" ${state.locked?'disabled':''}/></div>`).join('')}async function loadCard(){let card={};if(member)try{card=await rpc('gridiron_draft_prediction_card',{p_member_name:member})}catch{}questions(card);$('#dtIdentity').textContent=member?`Playing as ${member}`:'No sign-in required';$('#dtCardTitle').textContent=member?`${member.toUpperCase()} · BOLD PREDICTIONS`:'MAKE YOUR CALLS';$('#dtMemberName').value=member;$('#dtSubmit').disabled=state.locked;if(card.found)$('#dtMessage').textContent='Your card is saved. You can keep editing it until the draft starts.'}async function choose(){const n=$('#dtMemberName').value.trim();if(n.length<2){$('#dtMemberName').focus();$('#dtMessage').textContent='Enter your name first.';return}member=n;remember(n);await loadCard();$('#dtMessage').textContent=`Ready for ${member}. Fill every prediction, then file your card.`}async function board(){const out=await rpc('gridiron_draft_predictions_board');if(!out.locked){$('#dtBoard').innerHTML='';return}const lm=labels();$('#dtBoardNote').textContent='Locked. These are the original preseason receipts.';$('#dtBoard').innerHTML=(out.cards||[]).map(c=>`<article class="dt-member-card"><h3>${esc(c.member)}</h3>${Object.entries(c.answers||{}).map(([k,v])=>`<div class="dt-pick"><span>${esc(lm[k]||k)}</span><b>${esc(v)}</b></div>`).join('')}</article>`).join('')||'<p>No cards were filed.</p>'}async function render(){state=await rpc('gridiron_draft_predictions_state');member=remembered();$('#dtState').textContent=state.locked?`LOCKED · ${state.submitted}/${state.eligible} cards filed`:`OPEN · ${state.submitted}/${state.eligible} cards filed · ${state.total_points} points available`;await loadCard();if(state.locked)$('#dtMessage').textContent='Bold Predictions are locked. The league receipts are public below.';await board()}$('#dtUseName').onclick=choose;$('#dtMemberName').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();choose()}});$('#dtSubmit').onclick=async()=>{const msg=$('#dtMessage'),n=$('#dtMemberName').value.trim();if(n.length<2){$('#dtMemberName').focus();msg.textContent='Enter your name first.';return}member=n;remember(n);const inputs=[...document.querySelectorAll('.dt-question input')],answers={};for(const i of inputs){const v=i.value.trim();if(v.length<2){i.focus();msg.textContent='Use one player name for every prediction.';return}answers[i.dataset.key]=v}$('#dtSubmit').disabled=true;msg.textContent='Filing card…';try{const out=await rpc('gridiron_draft_predictions_submit_name',{p_member_name:member,p_answers:answers});member=out.member;remember(member);msg.textContent=`Card filed for ${out.member}. Receipts preserved.`;await render()}catch(e){msg.textContent=e.message}finally{if(!state?.locked)$('#dtSubmit').disabled=false}};render().catch(()=>{$('#dtState').textContent='Predictions unavailable';$('#dtMessage').textContent='Try again in a moment.'})})();
